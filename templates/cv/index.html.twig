<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>René Wetta - Choix du CV</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2) !important;
        }
    </style>
</head>
<body>

    <div class="container text-center mt-5">
        <h1 class="display-4 fw-bold text-primary">RENÉ WETTA</h1>
        <p class="lead mb-4">Bienvenue. Veuillez choisir le profil qui correspond à votre recherche.</p>

        <div class="row justify-content-center mt-5">
            
            {# BLOC STATIQUE - Développeur Web (vers site externe, non géré par le CvController) #}
            <div class="col-md-5 mb-4">
                <div class="card shadow-lg h-100 border-0">
                    <div class="card-body p-4">
                        <h5 class="card-title text-uppercase mb-3 fw-bold text-success">
                            Profil Développeur Web
                        </h5>
                        <p class="card-text text-muted">
                            Compétences techniques Full Stack (PHP, Symfony, JavaScript, Vue.js). Idéal pour les postes en équipe de développement.
                        </p>
                        <a href="/" class="btn btn-success btn-lg mt-3 w-100">
                            <i class="bi bi-code-slash me-2"></i> Vers le site JeDéveloppe.fr
                        </a>
                    </div>
                </div>
            </div>

            {# BOUCLE DYNAMIQUE pour les CV pilotés par le tableau AUTHORIZED_CVS du contrôleur #}
            {# La variable 'cv_list' doit contenir les données : file, title, description, icon, color #}
            {% for name, data in cv_list %}
                <div class="col-md-5 mb-4">
                    <div class="card shadow-lg h-100 border-0">
                        <div class="card-body p-4">
                            
                            {# Titre de la carte : On extrait le nom du profil (ex: "Manager Opérationnel") #}
                            <h5 class="card-title text-uppercase mb-3 fw-bold text-primary">
                                Profil {{ data.title|split('Voir le CV ')|last }}
                            </h5>
                            
                            {# Description tirée du tableau #}
                            <p class="card-text text-muted">
                                {{ data.description }}
                            </p>
                            
                            {# Lien et Bouton : Utilise la couleur et l'icône du tableau #}
                            <a href="{{ path('app_cv_show', {'cvName': name}) }}" class="btn {{ data.color }} btn-lg mt-3 w-100">
                                <i class="bi {{ data.icon }} me-2"></i> {{ data.title }}
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>